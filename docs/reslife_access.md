# Giving ResLife Access
For whoever needs to access, an administrator can go to the admin panel and get the auth code.  
When the session is over and the ResLife staff member has collected all data, change the auth code.

Send the ResLife staff member to `<SITE_URL>/application/manage` to login.